Example

Input:

5
5
3
6
7
1

Output:

1
3
5
6
7

#include<bits/stdc++.h>
using namespace std;

int main(void){
    
    int n;
    cin>>n;
    int arr[n];
    for(int i = 0; i < n; i++)
        cin>>arr[i];
    sort(arr, arr+n);              //sort(arr, arr+n, greater<int>()) is used for sorting in descending order 
    for(int i = 0; i < n; i++)
        cout<<arr[i]<<endl;
    return 0;
    
}
